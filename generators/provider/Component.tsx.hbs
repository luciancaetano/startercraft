import React from 'react';

import {{ pascalCase name }}ContextProvider from './{{ kebabCase name }}.context';
import { {{ pascalCase name }}Props, {{ pascalCase name }}ContextType } from './{{ kebabCase name }}.types';
import use{{ pascalCase name }}Model from './{{ kebabCase name }}.model';

function {{ pascalCase name }}(props: {{ pascalCase name }}Props) {
  const { children } = props;

  const contextValue: {{ pascalCase name }}ContextType = use{{ pascalCase name }}Model(props);

  return (
    <{{ pascalCase name }}ContextProvider value={contextValue}>
      {children}
    </{{ pascalCase name }}ContextProvider>
  );
}

export default React.memo({{ pascalCase name }});

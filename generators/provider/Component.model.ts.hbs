import { {{ pascalCase name }}Props } from './{{ kebabCase name }}.types';

function use{{ pascalCase name }}Model({ }: {{ pascalCase name }}Props) {

  return {
  };
}

export default use{{ pascalCase name }}Model;
